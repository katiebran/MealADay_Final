let express;_11b‍.x([["router",()=>router],["prefix",()=>prefix]]);_11b‍.w("express",[["default",["express"],function(v){express=v}]]);

       const router = express.Router();
       const prefix = '/';


/**
 * Hello world for the root
 */
router.get('/', function (req, res) {
  res.send({
    status: 'You have reached the root of the API!',
    repo: 'https://github.com/cgburgess/comp426-backend'
  })
});

